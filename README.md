# rename-images-boomit
 Adds a button to rename all images attached to a post using the post title, geotags images with saved latitude and longitude, and displays EXIF data in the media selection window.
